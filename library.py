# https://www.youtube.com/watch?v=7lmCu8wz
# 36:23
# For before a user hits production
# library .py

class Base:
    def foo(self):
        return 'foo'

* Tasks with arguments can be passed to a remote machine and run without oversight
* Output from commands can be parsed and have additional actions applied automatically or manually
* Output can be displayed as a web page
* Additional actions can be taken though the webpage either directly or forming a new command with can be copied and pasted into a terminal
looking into node-red, gauntlt, web servlets in js or python and maybe jenkins
